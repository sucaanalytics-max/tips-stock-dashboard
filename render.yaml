services:
  - type: web
    name: tips-stock-dashboard
    runtime: python
    region: singapore
    plan: free
    branch: main
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn app:server --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 --log-level info
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SUPABASE_URL
        value: postgresql://postgres:XQ4AJxtEbr41vAzs@db.bfafqccvzboyfjewzvhk.supabase.co:5432/postgres
    autoDeploy: true
